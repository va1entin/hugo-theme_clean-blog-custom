{{- $pages := where .Site.RegularPages "Type" "in" .Site.Params.mainSections -}}
{{- $notHidden := where .Site.RegularPages "Params.hidden" "!=" true -}}
{{- $filtered := ($pages | intersect $notHidden) -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $filtered = $filtered | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>{{ .Site.Params.nav.title }}</title>
        <link>{{ .Permalink }}</link>
        <description>{{ .Site.Params.description }}</description>
        <generator>Hugo -- gohugo.io</generator>{{ with .Site.Language.Locale }}
        <language>{{.}}</language>{{end}}{{ with .Site.Params.author_email }}
        <managingEditor>{{.}}{{ with $.Site.Params.default_author }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Params.author_email }}
        <webMaster>{{.}}{{ with $.Site.Params.default_author }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }} -->
        <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
        <lastBuildDate>{{ .Date.Format .Site.Params.dateFormat.published | safeHTML }}</lastBuildDate>{{ end }}
        {{- with .OutputFormats.Get "RSS" -}}
        {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
        {{- end -}}
        {{ range $filtered }}
        {{- $content := safeHTML (.Summary | html) -}}
        {{- if .Site.Params.rssFullContent -}}
            {{- $content = safeHTML (.Content | html) -}}
        {{- end -}}
        <item>
        <title>{{ .Title }}</title>
        <link>{{ .Permalink }}</link>
        <pubDate>{{ .Date.Format .Site.Params.dateFormat.published | safeHTML }}</pubDate>
        {{ with .Site.Params.author_email }}<author>{{.}}{{ with $.Site.Params.default_author }} ({{.}}){{end}}</author>{{end}}
        <guid>{{ .Permalink }}</guid>
        <description>
 {{ $content }}</description>
        </item>
        {{ end }}
    </channel>
</rss>
